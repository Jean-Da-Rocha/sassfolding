# App
APP_DEBUG=true
APP_ENV=local
APP_KEY=
APP_NAME="${COMPOSE_PROJECT_NAME}"
APP_URL="https://app.${COMPOSE_PROJECT_NAME}.test"

# Logging
LOG_CHANNEL=daily

# Database
DB_CONNECTION=mysql
DB_DATABASE="${COMPOSE_PROJECT_NAME}"
DB_HOST=mysql
DB_PASSWORD=hybridly
DB_USERNAME=laravel

# Mailpit
MAIL_ENCRYPTION=false
MAIL_FROM_ADDRESS="dev@${COMPOSE_PROJECT_NAME}.test"
MAIL_FROM_NAME="${COMPOSE_PROJECT_NAME}"
MAIL_HOST=mail
MAIL_MAILER=smtp
MAIL_PASSWORD=null
MAIL_PORT=1025
MAIL_USERNAME=null

# RustFS (S3-compatible storage)
AWS_ACCESS_KEY_ID=rustfs-root-user
AWS_BUCKET=media
AWS_BUCKET_POLICY=private
AWS_DEFAULT_REGION=us-east-1
AWS_ENDPOINT="https://storage.${COMPOSE_PROJECT_NAME}.test"
AWS_SECRET_ACCESS_KEY=rustfs-root-password
AWS_USE_PATH_STYLE_ENDPOINT=true
FILESYSTEM_DISK=s3
RUSTFS_CONSOLE_URL="https://rustfs.${COMPOSE_PROJECT_NAME}.test"

# Redis
CACHE_STORE=redis
QUEUE_CONNECTION=redis

REDIS_HOST=redis
REDIS_PASSWORD=null
SESSION_DRIVER=redis

# Octane
OCTANE_SERVER=frankenphp

# Vite
VITE_APP_NAME="${COMPOSE_PROJECT_NAME}"
